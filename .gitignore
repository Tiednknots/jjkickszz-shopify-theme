# Shopify Theme — JJ KICKSZZ
# This repository is connected directly to Shopify via GitHub integration.
# Only edit files here through the automated sync from the main workspace.
# Client customizations (config/settings_data.json) are auto-committed by Shopify.

*.DS_Store
*.log
